# EMMA Municipal Distress Monitoring System — Python Dependencies
# Install: pip install -r requirements.txt

# ---------------------------------------------------------------------------
# Phase 1 — Scraping & Discovery
# ---------------------------------------------------------------------------

# HTTP client — session management, connection pooling
requests>=2.31.0

# HTML parsing — for IssueView/Details and disclosure pages
beautifulsoup4>=4.12.0
lxml>=4.9.0          # Faster HTML parser backend for BeautifulSoup

# ---------------------------------------------------------------------------
# Phase 2 — Database
# ---------------------------------------------------------------------------

SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0    # PostgreSQL driver (not needed if using SQLite)
# alembic>=1.13.0          # Database migrations — add when schema stabilises

# ---------------------------------------------------------------------------
# Phase 3 — Configuration
# ---------------------------------------------------------------------------

python-dotenv>=1.0.0      # Load .env file for EMMA_STORAGE_DIR, API keys, etc.

# ---------------------------------------------------------------------------
# Phase 4 — AI Parsing
# ---------------------------------------------------------------------------

anthropic>=0.25.0         # Claude API client (claude-sonnet-4-6, claude-haiku-4-5)
pdfplumber>=0.10.0        # PDF text extraction (text-based PDFs)
pytesseract>=0.3.10       # OCR fallback for scanned/image-based PDFs
Pillow>=10.0.0            # Image handling required by pytesseract
pdf2image>=1.17.0         # Convert PDF pages to images for OCR
pydantic>=2.0.0           # Structured output validation for AI responses

# ---------------------------------------------------------------------------
# Development & Testing
# ---------------------------------------------------------------------------

# pytest>=7.4.0
# pytest-cov>=4.1.0
# responses>=0.24.0        # Mock HTTP responses in tests
# black>=23.0.0            # Code formatter
# ruff>=0.1.0              # Linter
